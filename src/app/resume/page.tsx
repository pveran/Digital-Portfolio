import { Navbar } from "@/components/navbar"

export const metadata = {
    title: "Resume",
    description: "Page shows my resume",
};

export default function Resume() {
    return (
        <>
            <Navbar />
            <h1>Resume</h1>
        </>
    )
}